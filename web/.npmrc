# Slice 1 / PR-1 audit (Spec 053): Tailwind v4 ships @tailwindcss/oxide native bindings.
# Add it here only if `pnpm install --frozen-lockfile` reports a blocked native build step.
onlyBuiltDependencies=[]