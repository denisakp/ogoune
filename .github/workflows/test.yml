name: Run Tests

on:
  push:
    branches:
      - dev
    paths:
      - '**/*.go'
      - 'go.mod'
      - 'go.sum'
      - 'web/**'
      - '.github/workflows/test.yml'
  pull_request:
    branches:
      - dev
    paths:
      - '**/*.go'
      - 'go.mod'
      - 'go.sum'
      - 'web/**'
      - '.github/workflows/test.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DB_DRIVER: sqlite
      SQLITE_PATH: /tmp/ogoune-ci.db
      DB_LOG_LEVEL: error
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'

      - name: cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: sqlc check (fail on drift)
        run: make sqlc-check

      - name: migrations drift check
        run: make migrations-drift-check

      - name: run tests
        run: go test -C . -v -race -timeout 120s ./...

  test-be-postgres:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'

      - name: cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: pre-pull postgres image
        run: docker pull postgres:16-alpine

      - name: dual-dialect tests (Postgres + SQLite via testcontainers)
        run: make test-be-pg

  test-be-sqlc-tags:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      SQLC_TAGS: "true"
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'

      - name: cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: pre-pull postgres image
        run: docker pull postgres:16-alpine

      - name: dual-dialect tests with SQLC_TAGS=true
        run: make test-be-pg

  test-be-sqlc-wave1:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      SQLC_API_KEY: "true"
      SQLC_USER: "true"
      SQLC_NOTIFICATION_CHANNEL: "true"
      SQLC_EXPIRY_NOTIFICATION_LOG: "true"
      SQLC_STATUSPAGE_SETTINGS: "true"
      SQLC_INCIDENT_DIAGNOSTICS: "true"
      SQLC_RESOURCE_CREDENTIAL: "true"
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'

      - name: cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: pre-pull postgres image
        run: docker pull postgres:16-alpine

      - name: dual-dialect tests with all Wave-1 flags ON
        run: make test-be-pg

  test-be-sqlc-wave3:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      # Pilot
      SQLC_TAGS: "true"
      # Wave 1
      SQLC_API_KEY: "true"
      SQLC_USER: "true"
      SQLC_NOTIFICATION_CHANNEL: "true"
      SQLC_EXPIRY_NOTIFICATION_LOG: "true"
      SQLC_STATUSPAGE_SETTINGS: "true"
      SQLC_INCIDENT_DIAGNOSTICS: "true"
      SQLC_RESOURCE_CREDENTIAL: "true"
      # Wave 2
      SQLC_COMPONENT: "true"
      SQLC_MAINTENANCE: "true"
      SQLC_NOTIFICATION: "true"
      SQLC_MONITORING_ACTIVITY: "true"
      SQLC_INCIDENT_EVENT_STEP: "true"
      # Wave 3
      SQLC_RESOURCE: "true"
      SQLC_INCIDENT: "true"
    steps:
      - uses: actions/checkout@v6

      - name: setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.1'

      - name: cache Go modules
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
            ~/go/bin
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: pre-pull postgres image
        run: docker pull postgres:16-alpine

      - name: dual-dialect tests with all Wave-3 flags ON
        run: make test-be-pg
