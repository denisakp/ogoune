# ogoune Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-01

## Active Technologies
- Go 1.25.1 (backend), TypeScript / Vue 3 (frontend) + GORM (persistence), Ant Design Vue (UI components), Vitest (frontend tests), Go testing stdlib (backend tests) (022-fix-heartbeat-waiting-state)
- SQLite (community) / PostgreSQL (hosted) — `last_ping_at` field exists in both, nullable (022-fix-heartbeat-waiting-state)

- Go 1.25.1 (backend tests), Docker 27 (builds), Shell (pipeline scripts) + GitLab CI/CD, Docker-in-Docker, yq v4.44.3, jq (021-gitlab-ci-workflows)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Go 1.25.1 (backend tests), Docker 27 (builds), Shell (pipeline scripts)

## Code Style

Go 1.25.1 (backend tests), Docker 27 (builds), Shell (pipeline scripts): Follow standard conventions

## Recent Changes
- 022-fix-heartbeat-waiting-state: Added Go 1.25.1 (backend), TypeScript / Vue 3 (frontend) + GORM (persistence), Ant Design Vue (UI components), Vitest (frontend tests), Go testing stdlib (backend tests)

- 021-gitlab-ci-workflows: Added Go 1.25.1 (backend tests), Docker 27 (builds), Shell (pipeline scripts) + GitLab CI/CD, Docker-in-Docker, yq v4.44.3, jq

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
;
